# 工作台配置

> 这份文件是给 AI 看的。以后想改工作台，直接跟 AI 说「给我的工作台加个喝水提醒」，
> 它会读这份配置接着改，不用重新聊一遍。**请不要删掉这个文件。**

生成时间：2026-08-28
最后更新：2026-08-28

---

## 一、用户画像

- **身份 / 主要在忙什么**：赛事策划（运动会 / 电竞 / 活动类赛事的组织与执行）
- **核心诉求**：一个手机端赛事策划工作台，集中管理「赛事时间 + 闹钟」「参赛人员（Excel 导入）」「赛程安排（比分 / 截图 / 链接）」
- **隐私敏感度**：一般（赛事数据多为工作性质，但建议仍走本地优先）
- **技术水平**：未明确（按「完全不懂代码」的默认假设交付，所有操作都在界面内完成）

## 二、功能清单

| # | 功能 | 标识 | 类型 | 侧边栏 | 说明 |
|---|---|---|---|---|---|
| 1 | 日历 | `calendar` | 本地 | 📅 日历 | 月历视图（含农历、节假日），点击日期添加时间+事项的待办；待办以深色小条直接显示在日期数字下方；可勾选完成 / 删除 / 按起始日（周日或周一）切换 |
| 2 | 赛事时间 | `matchtime` | 本地 | ⏰ 赛事时间 | 新增 / 删除赛事、行内编辑赛事名字、设定闹钟提醒（🔔） |
| 3 | 参赛人员 | `players` | 本地 | 👥 参赛人员 | 导入 Excel(.xlsx/.xls/.csv)，自动取表头；支持搜索、行内编辑、逐行删除、清空 |
| 4 | 赛程安排 | `schedule` | 本地 | 📋 赛程安排 | 上下排列各场对战；行内改战队名 + 对战时间；展开后填双方比分、上传对战截图、添加链接 |
| 5 | 设置 | `settings` | 本地 · 必备 | ⚙️ 设置 | 数据备份 / 恢复、双端同步开关、清空数据、关于 |

**用户明确拒绝过的功能**：周复盘（用户要求移除，2026-08-28）

## 三、风格参数

选定版本：极简（用户二选一选定）

```css
--bg:#f7f7f8;
--sidebar:#ffffff;
--sidebar-text:#3a3a3c;
--sidebar-active:#ececee;
--card:#ffffff;
--text:#1c1c1e;
--text-dim:#8e8e93;
--accent:#1c1c1e;
--radius:12px;
--shadow:0 1px 3px rgba(0,0,0,.06);
--font:-apple-system,"PingFang SC",system-ui,sans-serif;
```

- 品牌 emoji：🏆
- 工作台名：赛事策划工作台
- 短名称（桌面图标下的字）：赛事台
- 图标做法：纯色 + 文字（make_icon.py，深底 #1c1c1e + 白字「赛」）
- 用户在预览环节提过的偏好：极简、克制

## 四、数据结构

存储命名空间：`bysdash:`（敏感数据 `bysdash$secret:`，不进备份）

| Store key | 结构 | 归属功能 |
|---|---|---|
| `calendar_todos` | `[{id, date:'YYYY-MM-DD', time:'HH:MM', text, done}]` | 日历（数组，用 upsert/softDelete） |
| `match_events` | `[{id, name, datetime, reminder}]` | 赛事时间（数组，用 upsert/softDelete） |
| `alarm_fired` | `{ [eventId]: true }` | 赛事时间（已提醒标记，单值） |
| `players_columns` | `[列名...]` | 参赛人员（表头顺序，单值） |
| `players` | `[{id, 列1, 列2, ...}]` | 参赛人员（数组，用 upsert/softDelete） |
| `schedule` | `[{id, teamA, teamB, datetime, scoreA, scoreB, media:[{type:'image'|'link', value, title, ts}]}]` | 赛程安排（数组，用 upsert/softDelete） |
| `_lastBackup` / `_lastWrite` / `_lastPage` | `timestamp` / `timestamp` / `string` | 系统 |

## 五、联网配置

无（本工作台全部为本地功能，未使用任何 API key，index.html 里的 apiCard 已自动移除）。

## 六、部署

- **本地目录**：`C:\Users\admin\WorkBuddy\2026-08-28-17-02-00\赛事策划工作台`
- 层级：L0 未部署
- 平台：EdgeOne Pages
- 地址：未部署
- 部署方式：**从 Git 仓库导入**（工程中含 `edge-functions/api/sync.js` 与 `package.json` 的 `@edgeone/pages-blob` 依赖，拖文件夹直传不会触发构建，必须走 Git 导入）
- 已加到桌面：否
- 云同步配置：可选。用户在「设置 → 双端同步」里自助开启（需设用户名 + 密码，数据上云前请先导出备份）

## 七、工程状态

- 文件结构：三文件 + 本地 SheetJS（`xlsx.full.min.js` 已内联打包，离线可用，无 CDN 依赖）
- `index.html` 行数：约 900 行（未超 1500，暂不拆分）
- 上次验收脚本：通过 · 2026-08-28（validate_dashboard.py 零阻塞；smoke_test.js 15/15 通过）

## 八、已知限制（交付时务必转告用户）

1. **闹钟提醒**：纯网页无系统级后台推送。开启 🔔 后，仅在「本工作台页面保持打开」时到点才会触发系统通知（需用户授权 Notification 权限）+ 页面内横幅。关闭浏览器不会响。
2. **赛程截图体积**：截图经压缩（宽 800px / 质量 0.72）后存入本机浏览器，多图仍可能撑满 localStorage（约 5–10MB）；开启双端同步时单次同步数据上限 700KB，超量截图不会同步。建议适度使用，并定期到「设置」导出备份。
3. **Excel 导入**：取第一张表、第一行作表头；导入会替换当前参赛人员表（非追加）。

## 九、变更记录

| 日期 | 改了什么 |
|---|---|
| 2026-08-28 | 初始生成，5 个功能（赛事时间 / 参赛人员 / 赛程安排 / 周复盘 / 设置），极简风格，双端同步就绪 |
| 2026-08-28 | 移除「周复盘」功能（侧边栏、页面、JS 三处），功能数由 5 个降为 4 个 |
| 2026-08-28 | 新增「日历」功能（侧边栏首项）：月历视图（含 1900-2100 农历表）、点击日期添加带时间的待办、待办以深色小条直接显示在日期格子里，可勾选完成 / 删除；支持周一/周日起始切换 |
| 2026-08-28 | 修复日历 Bug：`init()` 每次 render 把 selDate 重置为今天，导致点其他日期后添加待办仍落到今天；改为一次性初始化守卫。并重排日历视觉：去掉沉重的「今日盒子」卡片与月份下拉，改为清爽标题栏（月份标题+翻月箭头+紧凑年份/周起始切换+「今天」按钮）；单元格用淡色高亮+细描边取代粗蓝描边，待办小条更克制 |
